<div id="message-<%= message.id || SecureRandom.uuid %>" class="group chat-message-enter" data-controller="message">
  <div class="<%= bubble_class %>">
    <%= render "chats/message_component/header", component: self %>

    <% if message.content.present? %>
      <div class="chat-bubble prose prose-sm <%= bubble_color %>" data-message-target="view">
        <%= markdown(message.content) %>
      </div>

      <% if editable_message? %>
        <%= render "chats/message_component/edit_form", message:, bubble_color: %>
      <% end %>
    <% end %>
  </div>

  <% if message.attachments.attached? %>
    <%= render "chats/message_component/attachments", message:, bubble_class:, image_attachments:, file_attachments: %>
  <% end %>
</div>
