<div class="mt-1 px-3 chat-message-enter">
  <div class="flex items-center gap-1.5 text-xs text-gray-500">
    <% if icon.present? %>
      <span class="<%= icon %>"></span>
    <% end %>
    <span><%= text %></span>
  </div>
</div>
