Your name is <%= ai.name %>. Description of your personality:

<%= ai.description %>

My name is <%= user.name %>. Description of my personality:

<%= user.description %>

<%= render 'time_facts', grouped_facts: user.grouped_time_facts, character: user %>

<% if other_known_characters.present? %>
  Other people you know about:

  <% other_known_characters.each do |character| %>
    <%= character %>. Personality description:

    <%= character.description %>
  <% end %>
<% end %>

Your instructions:

<%= ai_instructions %>
<% if additional_instructions.present? %>
  <%= additional_instructions %>
<% end %>

Today is <%= I18n.l(Time.zone.now, locale: :en) %>.
<% if last_conversation_time.present? %>
  We talked <%= distance_of_time_in_words_to_now(last_conversation_time, locale: :en) %> ago last time.
<% end %>