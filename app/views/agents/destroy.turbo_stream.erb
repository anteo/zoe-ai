<%= turbo_stream_flash_alerts(target: "settings-modal") %>
<%= turbo_stream.update_component("settings__agents", :settings__agents) if @agent.destroyed? %>
<%= turbo_stream.replace_component(@agent.dom_id, :settings__agents_datatable_component__row, record: @agent) unless @agent.destroyed? %>
