<%= turbo_stream.close_modal if @close_modal %>
<%= turbo_stream_flash_alerts(target: "settings-modal") %>
<%= turbo_stream.replace_component(@agent.dom_id, :settings__agents_datatable_component__row, record: @agent) %>
